% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_dist.R
\name{compare_dist}
\alias{compare_dist}
\title{This function generates visual comparisons for categorical, datetime,
and numerical columns by plotting bar plots and histograms.
It automatically adjusts the number of plots per page and pauses
when necessary to allow sequential viewing.}
\usage{
compare_dist(data_model, synthetic_data)
}
\arguments{
\item{data_model}{A list containing the original dataset and metadata.
It should have:
- \code{data_model$original}: The original dataset (data frame).
- \code{data_model$metadata}: Metadata containing column names and types.}

\item{synthetic_data}{A data frame containing the synthetic dataset,
structured similarly to \code{data_model$original}.}
}
\value{
The function does not return a value but generates multiple plots
for comparison.
}
\description{
This function generates visual comparisons for categorical, datetime,
and numerical columns by plotting bar plots and histograms.
It automatically adjusts the number of plots per page and pauses
when necessary to allow sequential viewing.
}
\details{
\itemize{
\item \strong{Categorical variables (\code{factor})}: Uses \code{bar_plot()} to compare category proportions.
\item \strong{Datetime variables (\code{datetime})}: Generates three plots:
\itemize{
\item Month distribution
\item Day of the week distribution
\item Hour distribution
}
\item \strong{Numeric variables (\code{integer}, \code{numeric})}: Uses histograms for density comparison.
}
}
\examples{
# Assuming `data_model` and `synthetic_data` are predefined:
compare_dist(data_model, synthetic_data)

}
